# Changelog

All notable changes to `@quinntyne/components` will be documented in this file.

This project uses [standard-version](https://github.com/conventional-changelog/standard-version) for automated versioning and changelog generation based on Conventional Commits.

The sections below will be populated when you run the release script:

- `fix:` for bug fixes (patch releases)
- `feat:` for new features (minor releases)
- `BREAKING CHANGE:` footer for breaking changes (major releases)
